// PowerAdminDashboard
// Re-uses the existing comprehensive AdminDashboard which already shows
// the full system-wide view (users, schedules, conflicts, rooms, requests,
// announcements, events, messages, charts). Power Admin sees everything.
//
// Future: extend with audit log feed and emergency override panel.
import AdminDashboard from './AdminDashboard';
export default AdminDashboard;
